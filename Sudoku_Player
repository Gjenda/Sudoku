class Player:
  def __init__(self, difficulty):
    self.timer= 0.0
    self.numofmistakes= 0
    self.grid= Grid(True, difficulty)
    self.filesaved= False
    
  def saveGame():
    #save csv file with player and grid 
    self.filesaved = True
  
  def addMistake()
  if self.numofmistakes <= 3 :
    self.numofmistakes +=
  else :
    self.grid.state = False
    
  def updateTimer(timer)
  self.timer=timer
  
  def deleteGame():
  #if self.filesaved == True :
    #delete csv file 
    #ile_path = '
    #if os.path.exists(file_path):
      #os.remove(file_path)de
  self.grid.deleteGrid()
